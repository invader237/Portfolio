apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: portfolio-prod
spec:
  type: ClusterIP
  selector: 
    app: frontend
  ports:
    - port: 80
      protocol: TCP
      nodePort: 3000
